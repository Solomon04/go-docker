{
  "name": "go-docker",
  "version": "1.0.0",
  "description": "Go Lang + GraphQL API",
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "config": {
    "commitizen": {
      "path": "git-cz"
    }
  },
  "husky": {
    "hooks": {
      "prepare-commit-msg": "exec < /dev/tty && git cz --hook || true"
    }
  },
  "devDependencies": {
    "cz-conventional-changelog": "^3.3.0",
    "git-cz": "^4.7.6",
    "husky": "^5.1.3"
  },
  "scripts": {
    "prepare": "husky install"
  }
}
